{% set haproxy = salt['grains.filter_by']({
    'Debian': {
    },
    'RedHat': {
    },
    'default': {
        'package': 'haproxy',
        'service': 'haproxy'
    }

}, merge=salt['pillar.get']('haproxy:lookup'), base = 'default') %}