{% set graylog = salt['grains.filter_by']({
    'Debian': {
        'repo': 'deb https://packages.graylog2.org/repo/debian/ stable 2.3',
    },
    'RedHat': {
        'repo': 'https://packages.graylog2.org/repo/el/stable/2.3/$basearch/',
    },
    'default': {
        'package': 'graylog-server',
        'service': 'graylog-server'
    }

}, merge=salt['pillar.get']('graylog:lookup'), base = 'default') %}